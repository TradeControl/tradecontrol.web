@page
@model TradeControl.Web.Pages.Admin.Template.ImageRemoveModel

@{
    ViewData["Title"] = "Remove Image";

    var embedded = false;
    if (Request?.Query.ContainsKey("embedded") == true)
    {
        embedded = string.Equals(Request.Query["embedded"], "1", StringComparison.OrdinalIgnoreCase)
            || string.Equals(Request.Query["embedded"], "true", StringComparison.OrdinalIgnoreCase);
    }

    if (embedded)
    {
        Layout = "/Pages/Shared/_EmbeddedLayout.cshtml";
    }

    var returnNode = (Request?.Query.ContainsKey("returnNode") == true)
        ? (Request.Query["returnNode"].ToString() ?? "Templates")
        : "Templates";

    var backHref = $"/Admin/Manager/Index?node={Uri.EscapeDataString(returnNode)}";
}

<div class="container-fluid py-2">
    @if (!embedded)
    {
        <h1 class="h4 mb-2">@ViewData["Title"]</h1>
        <div class="text-muted mb-3">@Model.Web_TemplateImage.TemplateFileName</div>
    }
    else
    {
        <div class="d-flex align-items-start justify-content-between mb-2">
            <div>
                <div class="fw-semibold">@ViewData["Title"]</div>
                <div class="text-muted small">@Model.Web_TemplateImage.TemplateFileName</div>
            </div>
            <div class="small text-muted">TemplateId: @Model.Web_TemplateImage.TemplateId</div>
        </div>
    }

    <div class="alert alert-warning" role="alert">
        This will unassign the image from the template. The image file will not be deleted from the system.
    </div>

    <div class="card shadow-sm">
        <div class="card-header bg-white d-flex align-items-center justify-content-between">
            <div class="fw-semibold">Confirm removal</div>

            @if (!embedded)
            {
                <a class="btn btn-outline-secondary btn-sm" href="@backHref">Back</a>
            }
        </div>

        <div class="card-body">
            <dl class="row mb-0">
                <dt class="col-sm-3">Image file</dt>
                <dd class="col-sm-9 text-break">@Model.Web_TemplateImage.ImageFileName</dd>

                <dt class="col-sm-3">Tag</dt>
                <dd class="col-sm-9 text-break"><code>@Model.Web_TemplateImage.ImageTag</code></dd>
            </dl>

            <form method="post" class="mt-3">
                @* Required for OnPost *@
                <input type="hidden" asp-for="Web_TemplateImage.ImageTag" />
                <input type="hidden" asp-for="Web_TemplateImage.ImageFileName" />
                <input type="hidden" asp-for="Web_TemplateImage.TemplateId" />
                <input type="hidden" asp-for="Web_TemplateImage.TemplateFileName" />

                @* Preserve UX context *@
                <input type="hidden" name="embedded" value="@(embedded ? "1" : "0")" />
                <input type="hidden" name="returnNode" value="@returnNode" />

                <div class="d-flex gap-2 flex-wrap">
                    <button type="submit" class="btn btn-danger">Remove</button>

                    <a asp-page="./Images"
                       asp-route-templateId="@Model.Web_TemplateImage.TemplateId"
                       asp-route-embedded="@(embedded ? "1" : null)"
                       asp-route-returnNode="@returnNode"
                       class="btn btn-outline-secondary">
                        Cancel
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

@if (embedded)
{
    <script>
        (function ()
        {
            try
            {
                window.parent.postMessage(
                    {
                        type: "tc.template.refresh",
                        scope: "template.images",
                        templateId: @Model.Web_TemplateImage.TemplateId
                    },
                    "*"
                );
            }
            catch
            {
            }
        })();
    </script>
}
