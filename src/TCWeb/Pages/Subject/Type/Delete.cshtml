@page
@model TradeControl.Web.Pages.Subject.Type.DeleteModel
@{ ViewData["Title"] = "Type Deletion"; }
<h1>@ViewData["Title"]</h1>

<div>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Subject_Type.SubjectType)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Subject_Type.SubjectType)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Subject_Type.CashPolarity)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Subject_Type.CashPolarity)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.NumberOfAccounts)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.NumberOfAccounts)
        </dd>
    </dl>
    <form method="post">
        <input type="hidden" asp-for="@Model.Subject_Type.SubjectTypeCode" />
        @{
            if (Model.NumberOfAccounts == 0)
            {
                <input type="submit" value="DELETE" class="btn btn-danger" />
            }
        }
        <a asp-page="./Index" class="btn btn-info">To List</a>
    </form>

    </div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
